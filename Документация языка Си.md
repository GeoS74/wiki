# Документация языка Си

Для установки мануалов:

```
sudo apt update

# базовый набор
sudo apt install manpages manpages-dev 

# русская версия
manpages-ru manpages-ru-dev

# POSIX расширение
sudo apt install manpages-posix manpages-posix-dev

# Специализированная документация
sudo apt install glibc-doc  # Детальная документация glibc
sudo apt install c-doc      # Дополнительно по Си

# проверить установленные пакеты
dpkg -l | grep manpages
```

## Как пользоваться:

### Базовые команды:

```bash
man printf          # документация на printf
man 3 printf        # явно указать раздел 3 (библиотечные функции)
man man             # документация на man 😄
```

### Секции man pages:

1 - пользовательские команды (ls, cp)
2 - системные вызовы (open, read)
3 - библиотечные функции (printf, malloc)
4 - специальные файлы (/dev/null)
5 - форматы файлов (passwd, hosts)
6 - игры
7 - соглашения и прочее
8 - команды администрирования

## Полезные опции `man`:

```bash
man -k printf       # поиск по ключевым словам
man -f printf       # показать все секции с printf
man -a printf       # показать все страницы по очереди
man -w printf       # показать путь к файлу документации
```

## Пример работы:

```bash
# Посмотреть документацию atoi
man atoi

# Внутри man:
/example           # поиск слова "example"
n                  # следующее вхождение
N                  # предыдущее вхождение
q                  # выход
```

### Настройка документации на русском

#### Сгенерировать русскую локаль:

```bash

# Редактировать файл локалей
sudo nano /etc/locale.gen

# Найти и раскомментировать строки:
ru_RU.UTF-8 UTF-8

# Сгенерировать локали
sudo locale-gen

# Обновить систему локалей
sudo update-locale
```

#### Проверить доступные локали

```bash
# все доступные локали
locale -a

# посмотреть текущую
locale
```

#### Настроить bash

```bash
# Правильно для Debian:
LANG=ru_RU.utf8 man printf
# или
LANGUAGE=ru man printf

# установка alias команды
alias manru='LANG=ru_RU.UTF-8 man' 
alias manen='LANG=en_US.UTF-8 man' 
source ~/.bashrc # перезагрузить терминал
manru printf # Использовать

# для всей сессии терминала
export LANG=ru_RU.UTF-8
# Теперь все man будут на русском
man printf
```